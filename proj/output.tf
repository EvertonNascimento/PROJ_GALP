/*6. The developed code will response the following outputs once “make apply” was run:
a. Private_instances_IP_addresses (Key/Value dictionary or map). The private IP address of
each attached Instance on the private subnets.
b. Bastion_Host_IP (string). The public IP address of the bastion host.galp.com
Site Reliability Engineering Test
3
c. SSH_key_content (string). The key to being used to SSH any instance.
d. Load_blanacer_HTTP_Content (string). Public DNS name to reach the created HTTP publication.
e. Usernames (key/value dictionary or map or string). The usernames to connect the instances
(even bastion host) . If the username is the same for all instances (even bastion host) then a
simple string output variable is enough.*/